<div class="root_view">
  <h1>Workouts</h1>

  <ul>
    <% @workouts.each do |workout| %>
      <li>
        <%= link_to workout.title, workout %>
      </li>
    <% end %>
  </ul>

  <%= link_to "New Workout", new_workout_path %>
</div>